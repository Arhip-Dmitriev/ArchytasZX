# ArchytasZX

ArchytasZX is a qufinite ZX-calculus engine and REPL for symbolically representing and rewriting quantum states and linear maps at symbolic qudit count and symbolic qudit dimension.

The Python package is currently named `qufzx`.

## Install

```
pip install -e ".[dev]"
```

## Test

```
pytest
```

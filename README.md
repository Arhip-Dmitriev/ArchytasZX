# qufzx

qufzx is a qufinite ZX-calculus engine and REPL for symbolically representing and rewriting quantum states and linear maps at symbolic qudit count and symbolic qudit dimension.

pip install -e ".[dev]"
pytest

